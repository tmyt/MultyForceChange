<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="net.refy.android.multyforcechange.MainActivity">


    <RadioGroup
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:layout_marginTop="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <RadioButton
            style="@style/MultyModeButton"
            android:background="@drawable/multy_mode_zoom_button"
            android:id="@+id/radioZoom" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <RadioButton
            style="@style/MultyModeButton"
            android:background="@drawable/multy_mode_docked_button"
            android:id="@+id/radioDocked" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <RadioButton
            style="@style/MultyModeButton"
            android:background="@drawable/multy_mode_mirror_button"
            android:id="@+id/radioMirror" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <RadioButton
            style="@style/MultyModeButton"
            android:background="@drawable/multy_mode_single_button"
            android:id="@+id/radioSingle" />
    </RadioGroup>
</RelativeLayout>
